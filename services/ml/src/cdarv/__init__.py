"""CDARV — Comparables-Driven ARV machine-learning foundation.

Consumes approved ("ideal") reports produced by the production Flowstate
API, learns comp selection/ranking from them, and emits shadow predictions
that never touch production underwriting.
"""

__version__ = "0.1.0"
